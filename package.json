{
  "name": "website-v1",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "npm run check:math && npm run check:homepage && npm run check:papers && npm run check:timestamps && npm run check:qnumbers && next build",
    "build:only": "next build",
    "start": "next start",
    "lint": "eslint",
    "check:math": "node scripts/check-math-render.mjs",
    "check:latex": "node scripts/check-notes-latex.mjs",
    "check": "npm run check:math && npm run check:latex && npm run check:homepage && npm run check:papers && npm run check:timestamps && npm run check:qnumbers",
    "check:homepage": "node scripts/check-homepage-years.mjs",
    "check:papers": "node scripts/check-paper-registry.mjs",
    "check:timestamps": "node scripts/check-timestamps.mjs",
    "check:qnumbers": "node scripts/check-question-numbers.mjs"
  },
  "dependencies": {
    "katex": "^0.16.28",
    "lucide-react": "^0.563.0",
    "next": "16.1.6",
    "qrcode": "^1.5.4",
    "react": "19.2.3",
    "react-dom": "19.2.3"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/qrcode": "^1.5.6",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.1.6",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
